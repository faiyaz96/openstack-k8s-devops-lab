apiVersion: v1
kind: Service
metadata:
  name: devops-demo-app
  namespace: demo
spec:
  selector:
    app: devops-demo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
